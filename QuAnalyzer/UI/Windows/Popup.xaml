<Controls:MetroWindow x:Class="QuAnalyzer.UI.Windows.Popup"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    WindowTitleBrush="{Binding CurrentProject.AccentColor,Source={x:Static Application.Current}}"
    TitleCharacterCasing="Normal"
    GlowBrush="{Binding CurrentProject.AccentColor,Source={x:Static Application.Current}}"
    mc:Ignorable="d" 
    Height="500" Width="800"
    x:Name="thisWindow">
    <Grid>
        <Frame x:Name="container" NavigationUIVisibility="Hidden" Navigated="Container_Navigated" />
    </Grid>
</Controls:MetroWindow>
